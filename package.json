{
  "name": "esm-test",
  "packageManager": "yarn@3.3.1",
  "private": true,
  "workspaces": [
    "scripts",
    "apps/*",
    "components/*",
    "functions/*",
    "bundler"
  ],
  "scripts": {
    "postinstall": "yarn dlx @yarnpkg/sdks vscode",
    "build": "yarn turbo build --filter=@flex-apps/people... --no-cache"
  },
  "devDependencies": {
    "@flexteam/mono-builder": "workspace:^",
    "turbo": "^1.6.3",
    "typescript": "^4.9.4"
  }
}
